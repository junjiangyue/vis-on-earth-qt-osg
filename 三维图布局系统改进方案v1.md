# 三维图布局系统改进方案

**目标周期：2天完成主要优化模块（性能、视觉表现、LOD控制、地形模拟）**  
**适用系统：基于 OSG 3.4.0 的三维图可视化系统**

---

## 一、改进概览

本次优化聚焦四个核心模块：

| 模块代号 | 模块名称              | 主要目的                                      |
|----------|-----------------------|-----------------------------------------------|
| A        | 边绘制性能优化         | 使用 Shader + VBO 提升大规模边绘制性能         |
| B        | 视觉表现增强           | 节点颜色/大小、边颜色/线宽由数据动态控制       |
| C        | LOD 支持               | 根据视角层级筛选展示图数据，提升性能与清晰度   |
| D        | 地形模拟与遮挡处理     | 在城市级别模拟高程变化，避免线条穿插障碍物     |

---

## 模块 A：边绘制性能优化（Shader + VBO）

### 目标结果
- 替换原CPU绘制流程，使用 GPU Shader 实现高性能绘制；
- 边颜色由两个节点颜色渐变；
- 线宽随边权重变化。

### 实现逻辑
1. 遍历所有可见边，提取插值点构建线段；
2. 每段线段写入如下结构体（LineVertex）：

   ```cpp
   struct LineVertex {
       osg::Vec3 pos;
       osg::Vec3 colorFrom;
       osg::Vec3 colorTo;
       float weight;
   };
   ```

3. 使用 VBO 上传所有线段点数据；
4. 用 Vertex + Fragment Shader 实现线条渐变、线宽控制；
5. 可选：使用 Geometry Shader 扩展线为 quad，实现宽度效果；
6. 启用 GL_BLEND 支持透明渲染。

### 会话目标
优化三维图布局系统的边绘制性能，从传统的逐边绘制方式转换为基于VBO+Shader的批量渲染。

### 完成的主要任务
1. **VBO+Shader渲染管线实现**
   - 在 `PerGraphParam` 类中添加了VBO相关成员变量和Shader程序
   - 实现了 `initEdgeShaders()` 方法，创建顶点和片段着色器
   - 实现了 `updateEdgeVBO()` 方法，将边数据填充到VBO中

2. **保留旧渲染逻辑**
   - 添加了 `mUseNewRenderer` 开关控制新旧渲染方式
   - 保持了原有边绘制逻辑的完整性

3. **弧线绘制优化**
   - 实现了基于边总长度的动态细分策略
   - 使用全局归一化参数实现完整弧线效果
   - 集成了地形高度检测，避免边穿过地面

### 关键决策和解决方案
- 使用GLSL 120版本确保与OSG 3.4.0兼容
- 通过权重模拟线宽，避免几何着色器的复杂性
- 实现了边绑定效果的弧线绘制算法

### 技术栈
- OSG 3.4.0
- C++11
- GLSL 120

### 修改的文件
- `vis4earth/graph_viser/graph_display.h`
- `vis4earth/graph_viser/graph_display.cpp`

---

## 模块 B：视觉表现增强（节点大小、颜色；边宽、渐变色）

### 目标结果
- 节点颜色由数据直接指定；
- 节点大小根据节点连接度自动缩放；
- 边线宽由权重决定；
- 边颜色为两端颜色插值。

### 实现逻辑

**节点处理**：
- 使用 `Node::color` 设置颜色；
- 读取node之后计算 `Node::degree`，归一化后调整 `Node::size`。

**边处理**：
- 颜色渐变与线宽控制在 Shader 中完成；
- 映射关系示例：权重 ∈ [0, 100] → 线宽 ∈ [0.5, 3.0]

---

## 模块 C：LOD 支持（分层展示）

### 目标结果
- 根据摄像机高度，动态调整图数据展示粒度；
- 4级视角范围控制展示密度。

### 视角等级定义

| 层级 | 高度范围（米）           | 描述     |
|------|--------------------------|----------|
| LOD 0 | > 12,338,400             | 全球级   |
| LOD 1 | 9,668,490 ～ 12,338,400 | 大陆级   |
| LOD 2 | 8,086,230 ～ 9,668,490  | 国家级   |
| LOD 3 | ≤ 8,086,230              | 城市级   |

### 筛选规则示例

```cpp
LODFilter filters[4] = {
  {80, 150}, {40, 100}, {20, 40}, {0, 0}
};
```

### 实现逻辑
- 获取相机高度后判断LOD；
- 按条件筛选节点/边数据；
- 使用 `setNodeMask()` 控制可见性。

### 会话目标
实现基于相机高度的LOD（Level of Detail）支持，确保节点、边和文字标签在不同层级间的同步切换。

### 完成的主要任务

1. **LOD数据结构设计**
   - 在 `PerGraphParam` 类中添加了LOD相关成员变量：
     - `allOriginalNodes` 和 `allOriginalEdges`：存储所有原始数据
     - `lodNodesData` 和 `lodEdgesData`：存储4个LOD层级的数据子集
   - 在构造函数中初始化LOD数据容器

2. **LOD数据预处理**
   - 实现了 `initializeLODData()` 方法：
     - 根据节点的 `level` 属性创建4个LOD层级的数据子集
     - LOD 0: 显示 `level == 0` 的节点/边
     - LOD 1: 显示 `level <= 1` 的节点/边  
     - LOD 2: 显示 `level <= 2` 的节点/边
     - LOD 3: 显示 `level <= 3` 的节点/边
     - 边的筛选基于其两个端点的level属性

3. **LOD切换机制**
   - 实现了 `setActiveLODDataSource()` 方法：
     - 根据目标LOD层级切换 `nodes` 和 `edges` 指针
     - 指向预处理好的数据子集
   - 实现了 `updateActiveLOD()` 方法：
     - 使用 `getCurrentLevel()` 确定目标LOD层级
     - 调用 `setActiveLODDataSource()` 切换数据源
     - 调用 `update()` 重新渲染几何体
     - 调用 `cameraUpdate()` 更新文字标签

4. **数据加载集成**
   - 在 `loadGeoTypeGraph()` 和 `loadNoGeoTypeGraph()` 中添加了 `initializeLODData()` 调用
   - 确保在图数据加载完成后立即初始化LOD数据

### 关键决策和解决方案

1. **数据预处理策略**
   - 选择在加载时预处理所有LOD层级数据，而非运行时动态筛选
   - 提高了运行时切换性能，降低了内存使用的动态性

2. **同步机制设计**
   - 几何体LOD通过 `updateActiveLOD()` 控制
   - 文字标签LOD继续使用现有的 `cameraUpdate()` 机制
   - 两者都依赖相同的 `getCurrentLevel()` 函数，确保同步

3. **数据源切换方式**
   - 通过智能指针切换，避免数据拷贝
   - 保持 `PerGraphParam::update()` 方法的签名和逻辑不变

### 技术栈
- OSG 3.4.0
- C++11
- 智能指针 (std::shared_ptr)

### 修改的文件
- `vis4earth/graph_viser/graph_display.h`
- `vis4earth/graph_viser/graph_display.cpp`

### 实现细节

**LOD数据管理架构调整：**
- 将LOD数据管理从 `PerGraphParam` 层面提升到 `GraphRenderer` 层面
- 与 `levelIndex` 和 `levelNodeIndex` 保持一致的管理方式
- 在每次加载图数据时清空并重新初始化所有LOD相关数据

**LOD数据存储结构：**
```cpp
// 在GraphRenderer类中
std::array<std::shared_ptr<std::map<std::string, Node>>, 4> lodNodesData; // 4个LOD层级的节点数据
std::array<std::shared_ptr<std::vector<Edge>>, 4> lodEdgesData; // 4个LOD层级的边数据
```

**LOD层级映射规则：**
```cpp
// getCurrentLevel() 返回值对应的显示规则
LOD 0 (height > 1.23384e+07): 显示 level == 0 的节点/边
LOD 1 (height > 9.66849e+06):  显示 level <= 1 的节点/边  
LOD 2 (height > 8.08623e+06):  显示 level <= 2 的节点/边
LOD 3 (height <= 8.08623e+06): 显示 level <= 3 的节点/边
```

**数据流程：**
1. **加载时**：
   - 清空 `levelIndex`、`levelNodeIndex` 和LOD数据容器
   - `GraphRenderer::initializeLODData()` 创建4个数据子集
   - 同时填充 `levelIndex` 和 `levelNodeIndex` 以供 `cameraUpdate` 使用

2. **运行时 (相机变化)**：
   - `GraphRenderer::updateActiveLOD(cameraHeight)` 被调用
   - `targetMaxNodeLevel = getCurrentLevel(cameraHeight)` 确定目标LOD
   - 直接设置 `graphParam->nodes` 和 `graphParam->edges` 指向 `lodNodesData[targetMaxNodeLevel]` 和 `lodEdgesData[targetMaxNodeLevel]`
   - `graphParam->update()` 被调用，使用新的数据源重新绘制节点和边的几何体
   - `cameraUpdate(graphName, cameraHeight)` 被调用，它也使用 `getCurrentLevel(cameraHeight)` 的结果，并基于 `levelIndex` 来更新标签

**同步保证：**
- 几何体和标签都依赖于相同的 `getCurrentLevel()` 函数
- 几何体使用 `lodNodesData/lodEdgesData`，标签使用 `levelIndex/levelNodeIndex`
- 两套数据都基于相同的 `Node::level` 属性进行筛选，确保完全同步

### 问题修复记录

**问题1：LOD切换后文字标签消失**
- **原因**：`syncSceneGraph` 方法中存在数据源不一致问题。`currentNodes` 基于 `levelNodeIndex` 计算，而 `graphParam->nodes` 是当前LOD数据，两者在LOD切换时不匹配。
- **解决方案**：修改 `syncSceneGraph` 方法，直接使用当前LOD的节点数据 (`graphParam->nodes`) 来查找和创建标签，确保标签数据与几何体数据一致。

**问题2：性能浪费 - 每次相机变化都重新绘制**
- **原因**：`updateActiveLOD` 方法没有检查LOD级别是否真正发生变化，导致每次调用都重新绘制几何体。
- **解决方案**：
  - 添加 `currentActiveLODLevel` 成员变量跟踪当前活动的LOD级别
  - 在 `updateActiveLOD` 中添加级别变化检查，只有当LOD级别真正变化时才重新绘制几何体
  - 无论LOD是否变化都调用 `cameraUpdate` 来更新标签（因为相机位置可能变化）

**修复后的优化效果：**
- LOD级别未变化时：只更新标签，不重新绘制几何体
- LOD级别变化时：重新绘制几何体并更新标签
- 标签数据与几何体数据完全同步，解决标签消失问题

**问题3：startStarAnimation访问冲突**
- **原因**：LOD切换后，`mEdgeGeometry` 的 StateSet 中的 Uniform 可能未正确初始化。当调用 `ss->getUniform("uLineDataTex")` 时，如果 Uniform 不存在会返回 nullptr，导致访问冲突。
- **解决方案**：
  - 在 `startStarAnimation` 方法中添加 Uniform 存在性检查
  - 如果 Uniform 不存在，先创建再使用
  - 使用安全的方式设置 Uniform 值，避免空指针访问
- **技术细节**：
  - 检查 `uLineDataTex`、`uTotalLines`、`uEnableHighlight` 等关键 Uniform
  - 使用 `addUniform` 创建缺失的 Uniform
  - 在设置值之前验证指针有效性

---

## 模块 D：地形模拟与遮挡处理（LOD 3）

### 目标结果
- 在 LOD 3（城市/山地）下模拟真实地形；
- 避免线条穿插障碍物，提高可视性。

### 实现逻辑
- 使用已有 `getBuildingHeightAtLatLon()` 函数；
- 每条边高度设置为：
  ```cpp
  interpolatedPos.z() = max(height, sin(π * t) * maxHeight);
  ```
- 其他LOD层级简化处理或不处理；
- 可加入统一抬高量（如 +300 米）

---

## 三、场景组织建议

推荐结构如下：

```
SceneRoot
├── LODGroup[0]  // 全球级数据
├── LODGroup[1]  // 大陆级数据
├── LODGroup[2]  // 国家级数据
└── LODGroup[3]  // 城市级数据
    ├── edgeGroup
    ├── nodeGroup
    └── labelGroup
```

每个 LODGroup 单独管理可见数据，切换时性能更优。

---

## 四、开发时间表

| 日期     | 模块  | 任务内容                                               |
|----------|--------|--------------------------------------------------------|
| 第一天   | A + B  | 重构边线Shader绘制；实现节点颜色/大小/边线宽渐变     |
| 第二天   | C      | 实现摄像机LOD检测；构建分层结构；按权重/度筛选数据   |
| 第三天   | D      | 整合地形高度处理；城市级线条抬高避障；整体调试优化   |

---

## 五、详细开发提示词

**通用提示：**
在开始每个模块的编码前，请再次详细阅读改进方案中对应模块的"目标结果"和"实现逻辑"。在编码过程中，请注意代码的可读性和维护性，并遵循你在 `<custom_instructions>` 中提到的KISS和SOLID原则。

---

**提示词 1：实现模块 A - 边绘制性能优化 (Shader + VBO)**

**目标文件**: `vis4earth/graph_viser/graph_display.cpp` (主要修改 `GraphRenderer::PerGraphParam::update()` 方法), 以及新的Shader文件 (GLSL)。

**具体任务**:
"请在 `GraphRenderer::PerGraphParam::update()` 方法中重构边的绘制逻辑，以实现基于Shader和VBO的高性能绘制：

1.  **清理旧逻辑**: 移除或注释掉当前 `PerGraphParam::update()` 中绘制线段的旧代码（例如，遍历边并逐个创建几何体或使用 `osg::DrawArrays(osg::PrimitiveSet::LINES, ...)` 的部分，如果它不是基于VBO和自定义Shader的）。
2.  **顶点数据准备**:
    *   遍历 `*(this->edges)`（确保只处理可见边 `edge.visible`）。
    *   对于每条边，获取其起点 (`edge.from`) 和终点 (`edge.to`) 节点的颜色（从 `this->nodes->at(edge.from).color` 和 `this->nodes->at(edge.to).color`）和位置。
    *   获取边的权重 (`edge.weight`)。
    *   根据边的细分点 (`edge.subDivs`) 生成线段序列。如果 `edge.subDivs` 为空，则边是一条直线段。你需要将这些点转换为世界坐标（球体坐标）。
    *   创建一个数据结构（或直接填充OSG数组）来存储每个顶点的信息，应包含：位置 (`osg::Vec3`)、起始节点颜色 (`osg::Vec3`或`osg::Vec4`)、结束节点颜色 (`osg::Vec3`或`osg::Vec4`)、边权重 (`float`)。
3.  **VBO构建**:
    *   将上述收集到的所有顶点数据填充到 `osg::Vec3Array` (位置), `osg::Vec4Array` (颜色 - 你可能需要两个颜色属性，或者在shader中根据一个插值因子和两个端点颜色计算), 和 `osg::FloatArray` (权重) 中。
    *   创建一个新的 `osg::Geometry` 对象 (`newGeom`)。
    *   将这些数组设置为 `newGeom` 的顶点属性数组。例如, `newGeom->setVertexAttribArray(location, array, binding)`。确保为每个属性选择一个唯一的 `location` 索引。
    *   使用 `newGeom->addPrimitiveSet(new osg::DrawArrays(osg::PrimitiveSet::LINES, 0, totalVertices))` 来指定绘制模式。
    *   将 `newGeom` 添加到一个新的 `osg::Geode` (`lineGeode`) 中，并将此 `geode` 添加到 `this->edgeNodegrp` (或 `this->grp`)。
4.  **Shader编写 (Vertex Shader)**:
    *   接收顶点属性：`layout(location = 0) in vec3 aPos;`, `layout(location = 1) in vec4 aColorFrom;`, `layout(location = 2) in vec4 aColorTo;`, `layout(location = 3) in float aWeight;` (位置和名称可自定义)。
    *   传递给Fragment Shader：`out vec4 vColorFrom; out vec4 vColorTo; out float vWeight;` (以及其他需要的，如插值因子 `vTexCoord` 如果你想基于线段长度进行颜色插值)。
    *   计算 `gl_Position = gl_ModelViewProjectionMatrix * vec4(aPos, 1.0);`
5.  **Shader编写 (Fragment Shader)**:
    *   接收来自Vertex Shader的varying变量：`in vec4 vColorFrom; in vec4 vColorTo; in float vWeight;`。
    *   实现颜色渐变：你需要一个插值因子（例如，基于线段内的归一化坐标，这可能需要额外传递或在FS中计算）。一个简单的方法是 `vec4 finalColor = mix(vColorFrom, vColorTo, interpolationFactor);`。
    *   线宽控制：改进方案提到在Shader中控制线宽。如果未使用Geometry Shader，标准OpenGL线宽是通过 `gl_LineWidth` (在VS或GS中设置) 或 `osg::LineWidth` 状态属性。如果要在FS中模拟宽度，通常涉及丢弃像素或特殊渲染技术，这比较复杂。若采用Geometry Shader，则GS负责扩展线为四边形。
    *   输出最终颜色：`fragColor = finalColor;`。
6.  **Shader集成**:
    *   创建 `osg::Program`，加载并附加Vertex和Fragment Shader。
    *   在 `lineGeode->getOrCreateStateSet()` 中设置该 `osg::Program`。
    *   确保绑定顶点属性位置与Shader中 `layout(location = ...)` 一致，可以使用 `program->addBindAttribLocation("aPos", 0);` 等。
    *   启用 `GL_BLEND` 以支持透明度：`states->setMode(GL_BLEND, osg::StateAttribute::ON);` 并设置合适的 `osg::BlendFunc`。
7.  **(可选) Geometry Shader**: 如果选择使用GS实现线宽，GS会接收线段的两个顶点，计算并输出四边形的四个顶点，同时传递颜色和权重等属性给Fragment Shader。"

---

**提示词 2：实现模块 B - 视觉表现增强**

**目标文件**: `vis4earth/graph_viser/graph_display.cpp` (主要在数据加载和 `GraphRenderer::PerGraphParam::update()` 部分), `vis4earth/graph_viser/graph_display.h` (可能需要修改 `GraphRenderer::Node` 结构体)。

**具体任务**:
"请修改图数据加载和节点渲染逻辑，以实现节点和边（部分已在模块A中处理）的动态视觉表现：
1.  **节点属性 (在 `GraphRenderer::Node` 结构体 - `graph_display.h`)**:
    *   确保 `GraphRenderer::Node` 结构体包含 `int degree;` 成员。
    *   （颜色 `color` 和大小 `size` 成员已存在）。
2.  **节点度计算和大小调整 (在 `GraphRenderer::loadGeoTypeGraph()` 和 `GraphRenderer::loadNoGeoTypeGraph()` 中)**:
    *   在节点和边数据完全加载后（即 `myGraph->set(read_nodes, allEdges);` 之后，或将 `VIS4Earth::Graph` 转换为 `std::map<std::string, Node>` 和 `std::vector<Edge>` 之后）。
    *   遍历所有边，计算每个节点的度数（出度和入度之和），并存储在对应 `Node` 对象的 `degree` 成员中。
    *   遍历所有节点，根据其 `degree` 计算并更新其 `size` 成员。你需要：
        *   找到最小和最大的度数值。
        *   设计一个归一化函数（例如，线性插值 `normalized_degree = (degree - min_degree) / (max_degree - min_degree);`）。
        *   将归一化后的度数映射到一个合适的视觉大小范围（例如，`node.size = min_visual_size + normalized_degree * (max_visual_size - min_visual_size);`）。
3.  **节点渲染 (在 `GraphRenderer::PerGraphParam::update()` 中)**:
    *   当创建节点几何体（例如 `osg::Sphere`）时，使用 `itr->second.size * scaling_factor * nodeGeomSize` 来设置其实际大小，确保 `itr->second.size` 生效。
    *   确保节点的颜色是直接从 `itr->second.color` 设置的。
4.  **边视觉属性 (主要由模块 A 的 Shader 实现)**:
    *   确认在模块 A 的Shader中，边的颜色确实是根据其两端节点的颜色进行插值生成的。
    *   确认在模块 A 的Shader中（或通过GS/LineWidth），边的宽度是根据其 `weight` 属性动态调整的。实现方案中提及的映射关系，例如：权重 `∈ [0, 100] →` 线宽 `∈ [0.5, 3.0]`。你需要根据实际的权重范围设计这个映射函数。"

---

**提示词 3：实现模块 C - LOD 支持 (分层展示)**

**目标文件**: `vis4earth/graph_viser/graph_display.cpp`, `vis4earth/graph_viser/graph_display.h`。

**具体任务**:
"请在 `GraphRenderer` 类中实现LOD（Level of Detail）支持，根据相机高度动态调整图数据的展示粒度：
1.  **LOD高度定义 (在 `GraphRenderer::getCurrentLevel()` - `graph_display.cpp`)**:
    *   核对并确保此函数中的相机高度阈值与改进方案中定义的四个LOD层级精确匹配：
        *   LOD 0: `height > 12338400.0`
        *   LOD 1: `height > 9668490.0` (且 `≤ 12338400.0`)
        *   LOD 2: `height > 8086230.0` (且 `≤ 9668490.0`)
        *   LOD 3: `height ≤ 8086230.0`
2.  **LOD数据准备与筛选 (修改数据加载或新增处理函数)**:
    *   在 `GraphRenderer::loadGeoTypeGraph()` (或 `loadNoGeoTypeGraph`，或一个专门的后处理函数) 中，你需要根据节点的属性（如 `level`、`degree`）和边的属性（如 `weight`）预先将节点和边筛选并分配到不同的LOD层级数据集中。
    *   方案提到"按权重/度筛选数据"。你需要为此定义具体的规则。例如：
        *   LOD 0 (全局级): 只显示 `level == 0` (如果 `Node::level` 属性适用) 或度数最高的N个节点，以及连接它们的权重最高的M条边。
        *   LOD 1 (大陆级): 显示 `level <= 1` 或更多节点/边。
        *   ...
        *   LOD 3 (城市级): 显示 `level <= 3` 或在视锥范围内满足特定条件的所有节点/边。
    *   修改 `GraphRenderer::levelNodeIndex` （目前是 `std::array<std::vector<Node>, 4>`）和 `GraphRenderer::levelIndex`（目前是 `std::array<std::vector<std::string>, 4>`）来存储每个LOD层级实际应该显示的节点ID或Node对象。或者，引入新的结构如 `std::array<std::shared_ptr<std::vector<Edge>>, 4> lodEdges;` 来存储每个LOD层级的边。
    *   **注意**: 当前的 `PerGraphParam::generateHierarchicalGraphs` 方法似乎是基于聚类的另一种LOD。方案中的模块C描述的LOD是基于相机高度和数据属性筛选。你需要澄清这两者关系，模块C可能需要独立实现其数据筛选逻辑。
3.  **场景组织和LOD切换 (在 `GraphRenderer::cameraUpdate()` 和 `GraphRenderer::PerGraphParam::update()`)**:
    *   考虑在 `GraphRenderer::PerGraphParam` 中为每个LOD层级创建一个 `osg::Group`，例如 `osg::ref_ptr<osg::Group> lodGroups[4];`。这些Group将作为 `PerGraphParam::grp` 的子节点。
    *   在 `GraphRenderer::PerGraphParam::update()` 中，不再直接向 `edgeNodegrp` 或 `grp` 添加所有几何体，而是根据当前的LOD层级（可以作为参数传递给 `update`，或使其能够访问 `GraphRenderer` 的 `currentLevel`）来决定将节点和边几何体构建到哪个 `lodGroups[currentLOD]` 中。
    *   在 `GraphRenderer::cameraUpdate()` 中：
        *   当 `currentLevel` 改变时，调用 `PerGraphParam::update()` （或一个专门的LOD更新函数）来重建或更新当前LOD的几何体。
        *   同时，需要管理 `lodGroups` 的可见性。可以通过 `lodGroups[i]->setNodeMask(0)` 隐藏非活动LOD，`lodGroups[currentLOD]->setNodeMask(0xFFFFFFFF)` 显示活动LOD。或者，通过从父节点 `PerGraphParam::grp` 中移除旧的LOD Group并添加新的。
4.  **同步 `currentNodes` 和标签**:
    *   在 `GraphRenderer::cameraUpdate()` 中，当LOD层级确定后，`currentNodes` 和 `currentLevelLabels` 应该被填充为当前活动LOD层级的节点数据。这是 `updateLabelLists()` 和 `syncSceneGraph()` 的基础。
    *   `updateLabelLists()` 和 `syncSceneGraph()` 可能需要调整以适应新的LOD数据结构。
5.  **性能**:
    *   目标是仅在LOD层级切换时执行主要的数据重建。如果仅相机移动但LOD不变，应依赖视锥剔除（如果已实现）或避免不必要的重建。"

---

**提示词 4：实现模块 D - 地形模拟与遮挡处理 (LOD 3)**

**目标文件**: `vis4earth/graph_viser/graph_display.cpp` (主要在 `GraphRenderer::PerGraphParam::update()` 方法内)。

**具体任务**:
"请在 `GraphRenderer::PerGraphParam::update()` 方法中，针对LOD 3（城市/山地级别）优化边的绘制，以模拟地形起伏并避免线条穿插：
1.  **LOD 3 条件**:
    *   在边的几何顶点生成循环（特别是高度计算部分）之前，需要获取当前的LOD层级。这个地形模拟和抬高逻辑**仅应**在当前为LOD 3时执行。你可以通过 `this->graphTypeIndex` （如果它能反映LOD层级，但更可能是需要从 `GraphRenderer` 主类获取信息）或传递给 `PerGraphParam::update` 的一个参数来判断。
2.  **边顶点高度计算**:
    *   当前代码中已有类似的高度计算逻辑：
        `interpolatedPos.z() = std::max(getBuildingHeightAtLatLon(interpolatedPos.x(), interpolatedPos.y()), float(maxHeight * sin(osg::PI * globalT)));`
    *   根据改进方案模块 D，公式为 `interpolatedPos.z() = max(terrain_or_building_height, sin(osg::PI * t_normalized_along_edge) * arch_max_height);`
        *   `terrain_or_building_height`: 这是通过 `getBuildingHeightAtLatLon(interpolatedPos.x(), interpolatedPos.y())` 获取的值。
        *   `arch_max_height`: 这是弧形的最大高度。当前代码中的 `maxHeight`（即 `edge.maxHeight`）似乎是此值。请检查 `edge.maxHeight` 的计算逻辑（基于 `maxRequiredAmplitude` 或固定值 `100000.f`）是否符合预期。方案中此值未明确定义，可能需要根据边的长度或一个全局参数来设定，以达到良好的视觉效果。
        *   `t_normalized_along_edge`: 这是当前插值点在整条边上的归一化位置（0.0 到 1.0）。确保 `globalT` 的计算是正确的。
    *   此计算应该应用于LOD 3下的所有边（或可见边）的每个插值点。
3.  **统一抬高量 (可选)**:
    *   方案中提到可以加入统一抬高量（如 +300米）。如果需要实现，可以将此偏移量加到最终计算出的 `interpolatedPos.z()` 上：
        `final_z = calculated_z_from_step2 + 300.0f;`
        （注意单位是否一致）。
4.  **数据依赖**:
    *   `getBuildingHeightAtLatLon()` 依赖于 `this->heightMap`。确保 `heightMap` 已在 `GraphRenderer::loadGeoTypeGraph()` 中正确加载，并且 `GraphRenderer::PerGraphParam` 实例能够访问到它。
5.  **其他LOD层级**:
    *   对于非LOD 3的层级，边的Z值可以简化处理（例如，全部设为0，或者一个较小的固定高度，或者不使用复杂的地形跟随和弧线逻辑，只要是一个弧线，不会被地球遮挡）。"

---

## 六、开发总结

### 2024-03-21 边绘制连接修复

#### 完成内容
1. 修复了边的连接问题：
   - 修改了顶点生成逻辑，确保线段正确连接
   - 使用 `prevSpherePos` 存储上一个点的球面坐标
   - 每次生成两个顶点，形成连续的线段

2. 优化了代码结构：
   - 简化了顶点、颜色和权重的添加逻辑
   - 移除了不必要的条件判断
   - 保持了数据的一致性

#### 技术栈
- OpenSceneGraph 3.4.0
- GLSL 1.20
- C++11

#### 关键决策
1. 采用连续顶点对生成线段
2. 使用临时变量存储上一个点的坐标
3. 确保每个线段都由两个连续的顶点组成
4. 保持颜色和权重数据与顶点数据同步

#### 后续优化建议
1. 考虑添加线段宽度控制
2. 优化颜色渐变效果
3. 添加线段动画支持
4. 实现透明度控制

#### 修改文件
1. vis4earth/graph_viser/graph_display.cpp

---
