模块A：图的颜色，线宽，美化显示效果
目前的节点颜色和边的颜色设置逻辑是：
1. 节点颜色：
   - 在Node结构体中通过osg::Vec3 color字段存储
   - 默认使用固定颜色，没有考虑LOD级别的一致性
   - 缺乏从CSV文件读取颜色的功能

2. 边的颜色：
   - 在Edge结构体中通过LineVertex结构存储起点和终点颜色
   - 目前没有实现节点颜色的混合计算
   - 权重到线宽的映射不够灵活
   - 透明度设置是硬编码的

想要的效果：
1. 不同LOD级别，同一个节点的颜色不能变化
2. 支持从csv中读取节点颜色并设置
3. 边的颜色是两个节点颜色的混合
4. 边的权重对应线宽
5. 可以开启透明度，权重低的透明度低
6. 使用边绑定之后，颜色和线宽和透明度不能变
7. 颜色和线宽和透明度要在传递到shader中完成绘制

方案：


3. 改进措施：
   - 实现颜色持久化机制，确保LOD切换时颜色保持一致
   - 添加CSV颜色导入功能，支持多种颜色格式（RGB/RGBA/HEX）
   - 实现智能线宽映射：weight_normalized = (weight - min_weight) / (max_weight - min_weight)
   - 添加透明度控制：alpha = base_alpha * weight_normalized
   - 边绑定后锁定视觉属性
   - 优化Shader实现：
     ```glsl
     // 顶点着色器
     varying vec4 v_color;
     varying float v_alpha;
     
     void main() {
         v_color = mix(startColor, endColor, gl_TexCoord[0].x);
         v_alpha = alpha * weight_factor;
         gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
     }
     ```

模块B：已有的动画效果适配
目前有三个动画效果函数，但是存在问题。
目标效果：
高光光照：每个边从起点到终点，有一个高光点进行运动，动画重复播放，每一个高光点的速度相同，也就是会有不同的到达时间。
拖尾：每个边从起点到终点，有一个有拖尾的高光点进行运动，动画重复播放，每一个高光点的速度相同，也就是会有不同的到达时间。
着色流动：给每条边上设置箭头纹理，让箭头从起点运动到终点，动画重复播放，每一个箭头的速度相同，也就是会有不同的到达时间。

目前逻辑：
高光光照：
- 使用uniform变量控制高光点位置
- 所有边使用相同的时间参数
- 没有考虑边长度差异
- 缺乏速度一致性控制

拖尾：
- 基于高光光照扩展，添加了衰减效果
- 拖尾长度是固定的，不随边长变化
- 动画循环处理不够平滑
- 性能开销较大

着色流动：
- 使用1D纹理实现箭头效果
- 纹理坐标计算不考虑边长
- 箭头大小固定，不随边长变化
- 缺乏速度归一化处理

实现方案：
1. 统一动画参数管理
```cpp
struct AnimationParams {
    float baseSpeed;        // 基准速度
    float currentTime;      // 当前时间
    bool normalized;        // 是否速度归一化
    float tailLength;       // 拖尾长度比例
    float arrowScale;       // 箭头大小比例
};
```

2. 边长度归一化
```cpp
void normalizeEdgeSpeed(Edge &edge) {
    float length = calculateEdgeLength(edge);
    edge.speed = baseSpeed / length;  // 速度反比于长度
}
```

3. 改进Shader实现
```glsl
// 高光效果
float highlight = smoothstep(0.0, 0.2, 1.0 - abs(v_texCoord - u_time * v_speed));

// 拖尾效果
float tail = smoothstep(v_texCoord, v_texCoord + tailLength, u_time * v_speed);

// 箭头流动
vec2 arrowTexCoord = vec2(fract(v_texCoord - u_time * v_speed), 0.5);
```

4. 性能优化
- 使用GPU实例化减少draw calls
- 实现动画参数缓存
- 添加LOD级别的动画细节控制

模块C：边绑定加速
目前的边绑定逻辑是：

1. 计算边的兼容性
   - 遍历所有边对，计算几何兼容性
   - 计算角度兼容性
   - 计算可见性兼容性
   - 计算尺度兼容性
   
2. 迭代优化
   - 添加控制点进行细分
   - 计算弹簧力和静电力
   - 更新控制点位置
   - 应用平滑处理

3. 存在问题
   - 兼容性计算复杂度高 O(n²)
   - 大量重复计算
   - 内存占用大
   - 缺乏空间索引优化

加速方案：
1. 空间索引优化
```cpp
struct SpatialGrid {
    float cellSize;
    std::vector<std::vector<Edge*>> cells;
    
    void insert(Edge* edge) {
        int cellIndex = getCellIndex(edge->midpoint);
        cells[cellIndex].push_back(edge);
    }
    
    std::vector<Edge*> getNeighbors(Edge* edge) {
        std::vector<Edge*> neighbors;
        int cellIndex = getCellIndex(edge->midpoint);
        // 只需要检查相邻格子中的边
        for (int i = -1; i <= 1; i++) {
            for (int j = -1; j <= 1; j++) {
                int neighborIndex = cellIndex + i + j * gridWidth;
                if (isValidIndex(neighborIndex)) {
                    neighbors.insert(neighbors.end(), 
                        cells[neighborIndex].begin(), 
                        cells[neighborIndex].end());
                }
            }
        }
        return neighbors;
    }
};
```

2. 并行计算优化
- 使用OpenMP并行化兼容性计算
- 实现任务分块处理
- 添加动态负载均衡

3. 计算优化
- 实现增量式兼容性更新
- 添加早期剪枝条件
- 使用近似计算加速
- 缓存中间结果

4. 内存优化
- 实现稀疏矩阵存储
- 使用内存池管理控制点
- 添加数据压缩机制

预期性能提升：
- 兼容性计算时间减少70%
- 内存占用减少50%
- 总体性能提升3-5倍

只对lod1和lod2进行全局边绑定
lod3只对当前可见边绑定
Bug:lod2边绑定存在断线
bug:边绑定之后动画无效
bug:lod1 lod2节点文字标签显示不正确
bug:切换到lod3瞬间会全量显示边
改进：lod层级越高,节点大小越小
增加：高度为xxxx的时候，高度采用maxHeight计算

今天进行：颜色，线宽，透明度，lod节点设置