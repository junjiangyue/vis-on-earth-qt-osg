# 模块三_开发提示词

**修订后的提示词 3.2：实现模块 C - LOD 支持 (预处理数据子集，切换数据源)**

**目标文件**: `vis4earth/graph_viser/graph_display.cpp`, `vis4earth/graph_viser/graph_display.h`。

**具体任务**:
"请在 `GraphRenderer` 类中实现LOD（Level of Detail）支持。根据相机高度动态切换 `PerGraphParam` 使用的节点和边数据源，确保几何体（节点、边）和文字标签的LOD层级同步。

1.  **LOD高度与目标显示Node Level的映射 (在 `GraphRenderer::getCurrentLevel()` - `graph_display.cpp`)**:
    *   `GraphRenderer::getCurrentLevel(double cameraHeight)` 函数的返回值（例如，0, 1, 2, 3）将作为当前**目标显示的节点 `level` 值上限**。
        *   LOD 0 (函数返回0): 显示 `Node::level == 0` 的节点/边。
        *   LOD 1 (函数返回1): 显示 `Node::level <= 1` 的节点/边。
        *   LOD 2 (函数返回2): 显示 `Node::level <= 2` 的节点/边。
        *   LOD 3 (函数返回3): 显示 `Node::level <= 3` 的节点/边。

2.  **LOD数据准备与存储 (在 `GraphRenderer::PerGraphParam` 中)**:
    *   **修改 `PerGraphParam` 结构**:
        *   添加成员变量以存储所有原始节点和边：
            `std::shared_ptr<std::map<std::string, Node>> allOriginalNodes;`
            `std::shared_ptr<std::vector<Edge>> allOriginalEdges;`
        *   添加成员变量以存储每个LOD层级筛选后的节点和边数据子集：
            `std::vector<std::shared_ptr<std::map<std::string, Node>>> lodNodesData;` (size 4)
            `std::vector<std::shared_ptr<std::vector<Edge>>> lodEdgesData;` (size 4)
        *   `PerGraphParam` 的构造函数或一个初始化方法 (`PerGraphParam::initializeLODData(allNodes, allEdges)`) 将负责：
            1.  将传入的完整节点和边数据存储到 `allOriginalNodes` 和 `allOriginalEdges`。
            2.  遍历0到3（代表LOD层级/最大node.level）的每个层级 `currentMaxLevel`：
                *   创建一个新的 `std::map<std::string, Node>` 和 `std::vector<Edge>`。
                *   遍历 `allOriginalNodes`，如果 `node.level <= currentMaxLevel`，则将其复制到为当前 `currentMaxLevel` 创建的节点映射中。
                *   遍历 `allOriginalEdges`，如果其连接的两个节点（根据 `allOriginalNodes` 中的 `level` 属性）都满足 `node.level <= currentMaxLevel`，则将其复制到为当前 `currentMaxLevel` 创建的边向量中。
                *   将这些新创建的节点映射和边向量（通过 `std::make_shared`）存储到 `lodNodesData[currentMaxLevel]` 和 `lodEdgesData[currentMaxLevel]` 中。
    *   **`GraphRenderer` 层面 (用于标签)**:
        *   `GraphRenderer` 类中的 `levelIndex` (`std::array<std::vector<std::string>, 4>`) 和 `levelNodeIndex` (`std::array<std::vector<Node>, 4>`) 的填充逻辑保持不变：在 `GraphRenderer::loadGeoTypeGraph()` (或 `loadNoGeoTypeGraph`) 中，根据每个节点的 `Node::level` 属性，将其ID添加到 `levelIndex[Node::level]`，将其本身添加到 `levelNodeIndex[Node::level]`。这确保了标签LOD的数据源与节点的原始 `level` 属性一致。

3.  **新的LOD控制函数 (在 `GraphRenderer` 类中)**:
    *   **创建新函数**: `void GraphRenderer::updateActiveLOD(double cameraHeight)`。
        *   `int targetMaxNodeLevel = getCurrentLevel(cameraHeight);`
        *   获取 `PerGraphParam* graphParam = getGraph("LoadedGraph");`
        *   如果 `graphParam` 存在，则调用一个新的方法 `graphParam->setActiveLODDataSource(targetMaxNodeLevel);`。
        *   在 `setActiveLODDataSource` 之后，**显式调用 `graphParam->update();`** 来触发使用新数据源的重绘。
        *   **然后，调用现有的 `this->cameraUpdate("LoadedGraph", cameraHeight);`**。

4.  **`PerGraphParam::setActiveLODDataSource(int targetMaxLevel)`**:
    *   此方法将 `PerGraphParam` 内部的 `this->nodes` 和 `this->edges` 指针（或智能指针）切换到指向预处理好的数据子集：
        `this->nodes = this->lodNodesData[targetMaxLevel];`
        `this->edges = this->lodEdgesData[targetMaxLevel];`
    *   **此方法不直接调用 `this->update()`**。`update()` 的调用由 `GraphRenderer::updateActiveLOD` 控制。

5.  **`GraphRenderer::PerGraphParam::update()` 的职责**:
    *   **此函数签名和内部逻辑保持不变。** 它继续基于 `this->nodes` 和 `this->edges` 当前指向的数据来构建和渲染节点球体和边线段的几何体。由于 `setActiveLODDataSource` 已经切换了这些指针，`update()` 自然会使用对应LOD层级的数据。

6.  **调用 `updateActiveLOD`**:
    *   在OSG的更新回调或处理相机变化的事件处理函数中，调用 `this->updateActiveLOD(currentCameraHeight);`。

7.  **`GraphRenderer::cameraUpdate` 的职责**:
    *   继续专注于标签的LOD。它使用 `getCurrentLevel()` 的结果和预填充的 `levelIndex`/`levelNodeIndex` 来确定显示哪些标签。由于 `levelIndex`/`levelNodeIndex` 是基于原始 `Node::level` 属性填充的，并且 `getCurrentLevel()` 对几何体和标签返回相同的目标层级上限，这将确保同步。

**流程总结**:

1.  **加载时**:
    *   `GraphRenderer` 加载所有节点和边。
    *   `PerGraphParam::initializeLODData` 被调用，它会创建4个不同详细程度的节点和边数据子集，并存储在 `lodNodesData` 和 `lodEdgesData` 中。每个子集 `lodNodesData[k]` 包含所有 `node.level <= k` 的节点。
    *   `GraphRenderer` 也根据原始 `Node::level` 填充 `levelIndex` 和 `levelNodeIndex` 以供 `cameraUpdate` 使用。

2.  **运行时 (相机变化)**:
    *   `GraphRenderer::updateActiveLOD(cameraHeight)` 被调用。
    *   `targetMaxNodeLevel = getCurrentLevel(cameraHeight)` 确定目标LOD。
    *   `graphParam->setActiveLODDataSource(targetMaxNodeLevel)` 将 `graphParam->nodes` 和 `graphParam->edges` 指向 `lodNodesData[targetMaxNodeLevel]` 和 `lodEdgesData[targetMaxNodeLevel]`。
    *   `graphParam->update()` 被调用，使用新的数据源重新绘制节点和边的几何体。
    *   `cameraUpdate(graphName, cameraHeight)` 被调用，它也使用 `getCurrentLevel(cameraHeight)` 的结果，并基于 `levelIndex` 来更新标签。由于两者都依赖于 `getCurrentLevel()` 和 `Node::level`，它们将保持同步。

这种方法将LOD数据的准备工作集中在初始化阶段，运行时切换LOD层级主要是切换数据指针并触发重绘，`PerGraphParam::update` 的核心渲染逻辑无需修改。